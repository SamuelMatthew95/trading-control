// Canonical primitive lives in @/components/ui/empty-state — re-exported here
// so existing `./EmptyState` importers keep working without a second copy.
export { EmptyState, type EmptyStateProps } from '@/components/ui/empty-state'
