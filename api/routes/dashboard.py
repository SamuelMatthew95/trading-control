"""
Dashboard routes - DISABLED due to V1 model dependencies.

This module uses non-existent V1 models and bypasses SafeWriter.
Needs to be rewritten with clean models and SafeWriter integration.
"""

# TODO: Rewrite dashboard with clean models and SafeWriter integration
# Current implementation uses non-existent V1 models and bypasses write guards

router = None  # Disabled

# All previous functionality commented out due to V1 model dependencies
