# TEMPLATE ONLY — copy to secret.yaml (git-ignored) and fill in real values,
# or create directly:
#
#   kubectl -n trading-control create secret generic trading-control-secrets \
#     --from-literal=ALPACA_API_KEY=... \
#     --from-literal=ALPACA_SECRET_KEY=... \
#     --from-literal=GEMINI_API_KEY=...
#
# In a real cluster prefer External Secrets Operator / sealed-secrets so the
# values never live in a YAML file at all.
apiVersion: v1
kind: Secret
metadata:
  name: trading-control-secrets
  namespace: trading-control
  labels:
    app.kubernetes.io/name: trading-control
type: Opaque
stringData:
  ALPACA_API_KEY: ""
  ALPACA_SECRET_KEY: ""
  GEMINI_API_KEY: ""
  GROQ_API_KEY: ""
  ANTHROPIC_API_KEY: ""
