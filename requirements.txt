# NOTE: This repository intentionally uses a single dependency file.
# CI, local development, and tests install from this file so the environment is
# uniform and reproducible. Dev/test tools are inert in production unless invoked.

# ── Runtime dependencies (API, workers, MCP, integrations) ───────────────────
aiohttp==3.14.0
aiosqlite>=0.19.0
alpaca-py
asyncpg==0.29.0
fastapi==0.136.3
fastmcp>=3.2.0,<4
google-genai>=1.0.0
groq
openai>=1.0.0
gunicorn==23.0.0
pydantic==2.11.7
pydantic-settings>=2.0.0
python-dotenv==1.2.2
# 5.2.x, not 5.0.1: redis-py 5.0.1 introduced an async connection-pool
# regression where cancelled/timed-out async commands leak their pooled
# connection, draining a BlockingConnectionPool until get_connection raises
# ConnectionError("No connection available.") — redis/redis-py#2995, with the
# related pipeline-connection-leak fix in 5.0.2 (#3104) and the async pool /
# retry / timeout revamp tracked in #3008. See docs/troubleshooting/system-routes.md.
redis==5.2.1
requests>=2.32.2
schedule==1.2.0
structlog==23.2.0
uvicorn[standard]==0.38.0
websockets>=15.0.1,<16

# ── Observability (OpenTelemetry — inert unless OTEL_ENABLED=true) ───────────
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-instrumentation-redis>=0.48b0
opentelemetry-instrumentation-aiohttp-client>=0.48b0

# ── Database dependencies ─────────────────────────────────────────────────────
alembic>=1.13.0
pgvector>=0.4.2
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# ── Lint/format/type/test tooling ─────────────────────────────────────────────
coverage>=7.10.0
factory-boy>=3.3.0
faker>=20.0.0
fakeredis==2.34.1
httpx[socks]>=0.28.1
mypy==1.7.1
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov>=7.0.0
pytest-mock>=3.15.0
pytest-xdist>=3.8.0
ruff==0.15.13
